name: Development Wokflow - test, linting, QA

on: push

jobs:
  install-job:
    name: Configure Environment
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
        test-command:
          - test
          - lint
          - stylelint
          - type-check

    steps:
      - uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Cache node modules
        uses: actions/@cache@v2
        env:
          cache-name: cache-node-modules
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: Install Node.js dependencies
        if: steps.cache-step.outputs.cache-hit != 'true'
        run: npm ci

      - name: Run tests
        run: npm run ${{ matrtix['test-command'] }}


  # run-linters-job:
  #   name: Lint
  #   runs-on: ubuntu-latest
  #   needs: [install-job]

  #   steps:
  #     - uses: actions/checkout@v2

  #     - name: Retrieve Node Modules cache
  #       uses: actions/cache@v2
  #       env:
  #         cache-name: cache-node-modules
  #       with:
  #         path: ~/.npm
  #         key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
  #         restore-keys: |
  #           ${{ runner.os }}-build-${{ env.cache-name }}-
  #           ${{ runner.os }}-build-
  #           ${{ runner.os }}-

  #     - name: Lint TS, JS and Styles
  #       run: |
  #         npm run type-check
  #         npm run lint
  #         npm run stylelint

  # run-tests-job:
  #   name: Test
  #   runs-on: ubuntu-latest
  #   needs: [install-job]

  #   steps:
  #     - uses: actions/checkout@v2

  #     - name: Retrieve Node Modules cache
  #       uses: actions/cache@v2
  #       env:
  #         cache-name: cache-node-modules
  #       with:
  #         path: ~/.npm
  #         key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
  #         restore-keys: |
  #           ${{ runner.os }}-build-${{ env.cache-name }}-
  #           ${{ runner.os }}-build-
  #           ${{ runner.os }}-

  #     - name: Run unit tests
  #       run: npm test